module NewsHelper

	# def sti_news_path(news_type = "news", news = nil, action = nil)
	#   send "#{format_sti(action, news_type, news)}_path", news
	# end

	# def format_sti(action, news_type, news)
	#   action || news ? "#{format_action(action)}#{news_type.underscore}" : "#{news_type.underscore.pluralize}"
	# end

	# def format_action(action)
	#   action ? "#{action}_" : ""
	# end

end
