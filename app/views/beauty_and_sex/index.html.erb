<%= form_tag(beauty_and_sex_index_path, :method => "get", id: "search-form") do %>
  <%= text_field_tag :search, params[:search], placeholder: "Search" %>
  <%= submit_tag "Search", :name => nil %>
<% end %>

<br/>

<% @beauty_and_sex.each do |i| %>
 <%= i.tip_name.html_safe %> | <%= i.tip_description.html_safe %> | <%= i.tip_author.html_safe %>
 <br/>
<% end %>